version: "3.8"

services:
  db:
    image: mariadb:10.6
    container_name: dolibarr_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: dolibarr_root
      MYSQL_DATABASE: dolibarr
      MYSQL_USER: dolibarr
      MYSQL_PASSWORD: dolibarr_pass
    volumes:
      - db_data:/var/lib/mysql
    # Si quieres acceder al MySQL remoto, descomenta:
    # ports:
    #   - "3307:3306"

  dolibarr:
    image: tuxgasy/dolibarr
    container_name: dolibarr_app
    restart: always
    environment:
      DOLI_DB_HOST: db
      DOLI_DB_NAME: dolibarr
      DOLI_DB_USER: dolibarr
      DOLI_DB_PASSWORD: dolibarr_pass
    depends_on:
      - db
    ports:
      - "8080:80"   # <── AHORA SÍ PUBLICADO
    volumes:
      - ./htdocs:/var/www/html
      - ./documents:/var/www/documents

  adminer:
    image: adminer:latest
    container_name: dolibarr_adminer
    restart: always
    depends_on:
      - db
    ports:
      - "8081:8080"  # para poder abrir adminer desde el navegador

  caddy:
    image: caddy:latest
    container_name: caddy_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - dolibarr
      - adminer

volumes:
  db_data:
  caddy_data:
  caddy_config:
